# Copy this file to .env and fill in real values before running in production.
# For local dev the defaults in settings.py are already set to match these.

DJANGO_SECRET_KEY=replace-with-a-long-random-string
DJANGO_DEBUG=true

DB_NAME=adosx_db
DB_USER=adosx_db
DB_PASSWORD=replace-me
DB_HOST=localhost
DB_PORT=5432
